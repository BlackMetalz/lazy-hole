# Sample only, not ssh-able
hosts:
  - name: mysql-node-1      # Display name in TUI
    ip: 10.0.0.11          # IP address to SSH to
    ssh_user: kienlt        # SSH username
    # ssh_port: 22            # Optional, default 22
    ssh_key: ~/.ssh/id_rsa  # Optional, default use ssh-agent / ~/.ssh/id_rsa
    group: mysql-db
  - name: mysql-node-2
    ip: 10.0.0.12
    ssh_user: kienlt
    group: mysql-db
  - name: mysql-node-3
    ip: 10.0.0.13
    ssh_user: kienlt
    group: mysql-db
  - name: k8s-node-1
    ip: 10.0.0.1
    ssh_user: kienlt
    group: k8s
  - name: k8s-node-2
    ip: 10.0.0.2
    ssh_user: kienlt
    group: k8s
  - name: k8s-node-3
    ip: 10.0.0.3
    ssh_user: kienlt
    group: k8s
  - name: elasticsearch-1
    ip: 10.0.0.201
    ssh_user: kienlt
    group: es
  - name: elasticsearch-2
    ip: 10.0.0.202
    ssh_user: kienlt
    group: es
  - name: elasticsearch-3
    ip: 10.0.0.203
    ssh_user: kienlt
    group: es
